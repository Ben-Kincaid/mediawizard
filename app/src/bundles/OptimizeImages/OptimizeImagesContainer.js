import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class OptimizeImagesContainer extends Component {
    constructor(props) {
        super(props);
        console.log("PROPS");
       console.log(props);
    }

    render() {
        return (
            <p>OptimizeImagesContainer</p>
        )
    }
}

